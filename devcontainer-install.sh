#!/bin/sh

# Uncomment this section if you want to install an additional version of node using nvm
# ARG EXTRA_NODE_VERSION=10
# nvm install ${EXTRA_NODE_VERSION}

# [Optional] Uncomment this section if you want to install more VSCode extensions
# code --install-extension <your-extension-name@version>

# [Optional] Uncomment this section to install additional OS packages.
# apt-get update && export DEBIAN_FRONTEND=noninteractive \
#     && apt-get -y install --no-install-recommends <your-package-list-here>

# [Optional] Uncomment if you want to install more global node packages
# npm install -g <your-package-list -here>

# [Optional] Use shell scripting for advanced configuration
# Example
# cd /home/node
# wget -nc <url of your-specific-linux-amd64-package.deb>
# dpkg -i your-specific-linux-amd64-package.deb